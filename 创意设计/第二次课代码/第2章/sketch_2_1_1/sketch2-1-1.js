function setup() {

}

function draw() {
  // circle圆括号中的三个参数，(50, 50)表示圆的中心位置坐标，80表示圆的直径。
  circle(50,50,80);
  }


/*JS知识tips
1.注释
注释可以是以 // 开始的单行注释，
也可以是 \/* ... *\/ 结构的多行注释。

2.声明函数
function showMessage() {
  alert( 'Hello everyone!' );
}
function 关键字首先出现，然后是函数名，showMessage
然后是括号之间的 参数 列表（用逗号分隔，在上述示例中为空），
最后是花括号之间的代码（即“函数体”）。

3.使用函数
函数可以通过名称调用：
function showMessage() {
  alert( 'Hello everyone!' );
}

showMessage();
*/