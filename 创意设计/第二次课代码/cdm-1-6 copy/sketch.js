function setup() {
  createCanvas(width,height);
  // onresize();
}

function draw() {
  // push();
  // fill('#fffccc');
  // beginShape(null);
  // bezier(266,528,329,517,374,482,397,454);
  // bezier(397,454,410,548,376,556,364,625);
  // bezier(364,625,287,637,277,625,266,528);
  // endShape(close);
  // pop();
  background(200);
  circle(mouseX,mouseY,30);
}